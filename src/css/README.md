# Custom CSS

Use `src/css/custom.css` as the fixed project stylesheet entry point.

You can add more files in `src/css/` and declare them in:

- `portfolio-config.json` -> `custom.styles`
- page JSON -> `presentation.extraStyles`

If `custom.styles` is omitted, `assets/css/custom.css` is loaded by default.
