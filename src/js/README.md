# Custom JS

Use `src/js/custom.js` as the fixed project runtime entry point.

You can add more files in `src/js/` and declare them in:

- `portfolio-config.json` -> `custom.scripts`
- page JSON -> `presentation.extraScripts`

If `custom.scripts` is omitted, `assets/js/custom.js` is loaded by default.
